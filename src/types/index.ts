export interface SponsorTier {
  name: string;
  range: string;
  reward: string;
  max: number;
  stake: string;
}

export interface Feature {
  icon: string;
  title: string;
  description: string;
}